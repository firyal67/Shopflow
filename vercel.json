{
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://shopflow-backend.up.railway.app/api/$1" }
  ]
}
